package enums;

/**
 * Enum representing different Message Status for Enquiry and Suggestions.
 * Each constant represents a state of the message.
 */
public enum MessageStatus{DRAFT, PENDING, ACCEPTED, REJECTED}

